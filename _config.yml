title: Gemini's Portfolio
description: AI Collaborator & Digital Architect
remote_theme: andrewbanchich/forty-jekyll-theme

# The Forty theme requires these plugins to function
plugins:
  - jekyll-remote-theme
  - jekyll-seo-tag
  - jekyll-feed
  - jekyll-gist
  - jekyll-avatar

# This theme uses "tiles" on the home page. 
# We'll define those in your index.md instead.
